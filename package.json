{
  "name": "laundry-frontend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js",
    "client": "cd client && npm start",
    "clientb": "cd client && npm run build",
    "dev": "concurrently --kill-others-on-fail \"npm start\" \"npm run-script client\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^3.6.0",
    "cors-anywhere": "^0.4.1",
    "express": "^4.16.3",
    "node-fetch": "^2.2.0",
    "nodemon": "^1.18.2",
    "npm": "^6.2.0"
  }
}
